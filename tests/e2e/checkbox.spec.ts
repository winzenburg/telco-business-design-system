/**
 * Visual Regression Tests for Checkbox
 * Auto-generated by generate-vrt-baselines.js
 */

import { test, expect } from '@playwright/test';

const STORYBOOK_URL = 'http://localhost:6006';

async function navigateToStory(page: any, storyId: string, theme = 'light') {
  const url = `${STORYBOOK_URL}/iframe.html?id=${storyId}&viewMode=story&theme=${theme}`;
  await page.goto(url);
  await page.waitForLoadState('networkidle');
  await page.waitForTimeout(500); // Allow animations to settle
}

async function stabilizeVisuals(page: any) {
  // Disable animations and hide variable content
  await page.addStyleTag({
    content: `
      *, *::before, *::after {
        animation-duration: 0s !important;
        transition-duration: 0s !important;
      }
      .loading, .spinner, [data-testid*="loading"] {
        visibility: hidden !important;
      }
    `
  });
}

test.describe('Checkbox Visual Tests', () => {
  
  test('Default - light theme', async ({ page }) => {
    await navigateToStory(page, 'checkbox--default', 'light');
    await stabilizeVisuals(page);
    
    await expect(page.locator('#storybook-root')).toHaveScreenshot(
      'checkbox--default-light.png',
      {
        threshold: 0.2,
        animations: 'disabled'
      }
    );
  });
  
  test('Default - dark theme', async ({ page }) => {
    await navigateToStory(page, 'checkbox--default', 'dark');
    await stabilizeVisuals(page);
    
    await expect(page.locator('#storybook-root')).toHaveScreenshot(
      'checkbox--default-dark.png',
      {
        threshold: 0.2,
        animations: 'disabled'
      }
    );
  });
  test('Checked - light theme', async ({ page }) => {
    await navigateToStory(page, 'checkbox--checked', 'light');
    await stabilizeVisuals(page);
    
    await expect(page.locator('#storybook-root')).toHaveScreenshot(
      'checkbox--checked-light.png',
      {
        threshold: 0.2,
        animations: 'disabled'
      }
    );
  });
  
  test('Checked - dark theme', async ({ page }) => {
    await navigateToStory(page, 'checkbox--checked', 'dark');
    await stabilizeVisuals(page);
    
    await expect(page.locator('#storybook-root')).toHaveScreenshot(
      'checkbox--checked-dark.png',
      {
        threshold: 0.2,
        animations: 'disabled'
      }
    );
  });
  test('Disabled - light theme', async ({ page }) => {
    await navigateToStory(page, 'checkbox--disabled', 'light');
    await stabilizeVisuals(page);
    
    await expect(page.locator('#storybook-root')).toHaveScreenshot(
      'checkbox--disabled-light.png',
      {
        threshold: 0.2,
        animations: 'disabled'
      }
    );
  });
  
  test('Disabled - dark theme', async ({ page }) => {
    await navigateToStory(page, 'checkbox--disabled', 'dark');
    await stabilizeVisuals(page);
    
    await expect(page.locator('#storybook-root')).toHaveScreenshot(
      'checkbox--disabled-dark.png',
      {
        threshold: 0.2,
        animations: 'disabled'
      }
    );
  });
  test('With Error - light theme', async ({ page }) => {
    await navigateToStory(page, 'checkbox--with-error', 'light');
    await stabilizeVisuals(page);
    
    await expect(page.locator('#storybook-root')).toHaveScreenshot(
      'checkbox--with-error-light.png',
      {
        threshold: 0.2,
        animations: 'disabled'
      }
    );
  });
  
  test('With Error - dark theme', async ({ page }) => {
    await navigateToStory(page, 'checkbox--with-error', 'dark');
    await stabilizeVisuals(page);
    
    await expect(page.locator('#storybook-root')).toHaveScreenshot(
      'checkbox--with-error-dark.png',
      {
        threshold: 0.2,
        animations: 'disabled'
      }
    );
  });
});

// Interactive state tests for key components

test.describe('Checkbox Interactive States', () => {
  test('hover state', async ({ page }) => {
    await navigateToStory(page, 'checkbox--default');
    await stabilizeVisuals(page);
    
    const element = page.locator('checkbox').first();
    await element.hover();
    await page.waitForTimeout(100);
    
    await expect(page.locator('#storybook-root')).toHaveScreenshot(
      'checkbox--default-hover.png',
      { threshold: 0.2, animations: 'disabled' }
    );
  });
  
  test('focus state', async ({ page }) => {
    await navigateToStory(page, 'checkbox--default');
    await stabilizeVisuals(page);
    
    const element = page.locator('checkbox').first();
    await element.focus();
    await page.waitForTimeout(100);
    
    await expect(page.locator('#storybook-root')).toHaveScreenshot(
      'checkbox--default-focus.png',
      { threshold: 0.2, animations: 'disabled' }
    );
  });
});
